import express, { Request, Response, Router } from 'express';
import Person from '../models/Product.js';

const router: Router = express.Router();

router
    .post('/products')
   
    
export default router;